name: Astral Autopilot

on:
  schedule:
    # Runs once every 3 hours – adjust if you want
    - cron: "0 */3 * * *"
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  update-astral-data:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Run astral autopilot script
        run: node scripts/astral-autopilot.mjs

      - name: Commit updated astral data (if changed)
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            git config user.name "conspiraai-autopilot"
            git config user.email "actions@github.com"
            git add data/lunar-data.json
            git commit -m "chore: update lunar data"
            git push
          fi
